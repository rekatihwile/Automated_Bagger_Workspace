# External Repositories

This folder contains third-party code cloned from outside this project.

Current third-party repositories:

- `RAFT-Stereo/` official RAFT-Stereo checkout used by `workspace/stereo_raft.py`.

Avoid editing external repositories unless intentionally patching or updating a
third-party dependency. Project-specific integration code should live in
`workspace/`.
